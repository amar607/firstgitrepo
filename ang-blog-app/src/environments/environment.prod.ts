// Used for production builds (ng build --configuration production).
// Replace this with your actual deployed backend URL from Railway
// (Settings -> Networking -> Public Domain on the backend service).
export const environment = {
  production: true,
  apiUrl: 'https://firstgitrepo-production.up.railway.app'
};
